<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Estado cuando el botón está presionado -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/holo_green_light" /> <!-- Cambiar el color de fondo cuando se presiona -->
            <corners android:radius="8dp" /> <!-- Cambiar el radio de las esquinas si se desea -->
        </shape>
    </item>

    <!-- Estado cuando el botón tiene foco -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/holo_green_light" /> <!-- Cambiar el color de fondo cuando se enfoca -->
            <corners android:radius="8dp" /> <!-- Cambiar el radio de las esquinas si se desea -->
        </shape>
    </item>

    <!-- Estado por defecto (cuando el botón no está presionado ni enfocado) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/holo_green_dark" /> <!-- Cambiar el color de fondo por defecto -->
            <corners android:radius="8dp" /> <!-- Cambiar el radio de las esquinas si se desea -->
        </shape>
    </item>

</selector>
